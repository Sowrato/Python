# Biblioteca MATH
    # ceil()
    # floor()
    # trunc()
    # pow()
    # sqrt()
    # factorial()

from math import sqrt
num = int(input('Digite um número: '))
raiz = sqrt(num)
print(f'\n-> A raiz de {num} é igual a {raiz:.2f}\n')